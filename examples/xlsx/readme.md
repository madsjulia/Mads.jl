# XLSX examples

These examples demonstrate how to read spreadsheet ranges into data frames and dictionaries with `Mads.get_excel_data`.

Run the focused XLSX tests from an activated Mads environment:

```julia
import Mads
Mads.test("xlsx")
```

See the [XLSX example source](./xlsx.jl) and the [focused XLSX tests](./runtests.jl).
